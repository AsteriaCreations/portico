<x-screen-instructions :title="__('How to use Vouchers')">
    <p>{{ __('An append-only account-credit ledger — every row here is a real event: a voucher issued, or spent at check-in. There is no edit or delete for anyone, ever.') }}</p>
    <p>{{ __('To fix a mistake, issue an offsetting row (a negative amount cancels a prior grant) rather than trying to change the original — that keeps the ledger an honest record of what actually happened.') }}</p>
    <p>{{ __('Manager+ can view this list; only Admin+ can issue a new voucher.') }}</p>
</x-screen-instructions>
