<x-screen-instructions :title="__('How to use Registers')">
    <p>{{ __('Defines the physical cash drawers staff pick from at the top of the check-in page — add one per till you actually run.') }}</p>
    <p>{{ __('Deactivate a register that\'s retired rather than deleting it, so its shift history stays intact.') }}</p>
</x-screen-instructions>
