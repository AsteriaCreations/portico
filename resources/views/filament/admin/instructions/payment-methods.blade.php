<x-screen-instructions title="How to use Payment Methods">
    <p>The pick-list offered everywhere staff take payment — check-in, subscription purchases, the register box.</p>
    <p>A method's <strong>code</strong> locks once created (it's already stamped on historical records), so pick it carefully the first time — everything else stays editable.</p>
    <p>Check <strong>Requires register shift</strong> for a method that needs an open cash drawer to be selectable and that should count toward end-of-shift reconciliation (e.g. Cash) — leave it off for something like Venmo that doesn't touch the box.</p>
</x-screen-instructions>
