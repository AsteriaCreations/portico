*.log
.DS_Store
.env
.env.backup
.env.production
.phpactor.json
.phpunit.result.cache
/.claude/
/Examples/
/.codex
/.cursor/
/.idea
/.nova
/.phpunit.cache
/.vscode
/.zed
/auth.json
# A cert/key pair issued while set up for local HTTPS (mkcert, etc.) lands
# wherever the shell's current directory is -- easy to run from the repo
# root by habit. Keep them from ever being staged even so.
/*.pem
/node_modules
/package-lock.json
/public/build
/public/fonts-manifest.dev.json
/public/hot
/public/storage
/storage/*.key
# A drop folder for files fed to an importer command (a legacy roster export, say).
# They hold member data -- names, emails, dates of birth -- and must never be staged.
/storage/imports
/storage/pail
/vendor
/database/testing.sqlite
_ide_helper.php
Homestead.json
Homestead.yaml
Thumbs.db
/storage/screenshots
/.ai/
